depends:
- pipx

actions:
- action: command.run
  command: pipx
  args:
  - install
  - cfn-lint

- action: command.run
  command: pipx
  args:
  - runpip
  - cfn-lint
  - install
  - --upgrade
  - pydot
